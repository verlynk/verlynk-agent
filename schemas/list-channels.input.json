{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "list-channels input",
  "description": "Input schema for the Verlynk MCP list-channels tool. _context is auto-populated by the server — omit it.",
  "type": "object",
  "properties": {
    "profileId": {
      "type": "string",
      "format": "uuid",
      "description": "Optional profile (project) ID when listing channels for a non-default profile."
    },
    "_context": {
      "type": "object",
      "description": "Auto-populated by server. Do not pass manually.",
      "properties": {
        "userId": { "type": "string" },
        "orgId": { "type": "string" },
        "projectId": { "type": "string" },
        "profileId": { "type": "string" }
      }
    }
  },
  "additionalProperties": false
}
